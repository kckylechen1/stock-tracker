---
description: 开发前必读 - 文档管理和开发流程规范
---

# Stock Tracker 开发工作流

## 📂 技术文档位置

所有技术文档存放在 `.agent/docs/` 目录下（不会上传到 Git）：

```
.agent/docs/
├── product-summary.md           # 产品需求和解决方案
├── frontend-spec.md             # 前端开发规范
├── nlp-strategy-design.md       # NLP选股策略设计
└── nlp-strategy-dev-plan.md     # 开发计划和任务拆解
```

## 🔄 开发流程

### 1. 开发前
```bash
# 阅读相关设计文档
view_file .agent/docs/{相关文档}.md
```

### 2. 开发中
- 遵循 `frontend-spec.md` 中的 UI/UX 规范
- 参考 `product-summary.md` 确认需求
- 按 `nlp-strategy-dev-plan.md` 的阶段推进

### 3. 开发后 / 收工流程
- 提交代码到 dev 分支（本地 commit，积累后 push）
- 更新相关文档的状态
- **创建每日工作总结** `.agent/workflows/daily-summary-YYYY-MM-DD.md`
  - 记录当日完成的功能
  - 记录明日待办事项
  - 记录用户偏好或新发现

### 4. 每日开始
- 跟用户说**早安**
- 查看前一天的工作总结 `/daily-summary-{前一天日期}`
- 确认今日待办事项

## 📝 Git 提交规范

```bash
# 功能开发
git commit -m "Feature: 功能描述"

# Bug 修复
git commit -m "Fix: 问题描述"

# 优化改进
git commit -m "Improve: 优化描述"
```

### 分支管理
- `dev`: 日常开发分支
- `main`: 稳定版本，用于 Manus 部署
- 积累多个相关改动后再 push 到远程
- 功能完成后合并 dev → main

## 🛠️ 常用命令

```bash
# 启动开发服务器
npm run dev

# 提交代码（本地）
git add -A && git commit -m "描述"

# 推送到远程
git push origin dev

# 合并到 main 并部署
git checkout main && git merge dev && git push origin main
```

## 📋 新功能开发检查清单

- [ ] 阅读相关设计文档
- [ ] 在 dev 分支开发
- [ ] 代码本地测试通过
- [ ] commit 描述清晰
- [ ] 功能完成后更新文档状态
