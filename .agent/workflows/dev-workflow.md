---
description: 开发前必读 - 文档管理和开发流程规范
---

# Stock Tracker 开发工作流

## 📂 技术文档位置

所有技术文档存放在 `.agent/docs/` 目录下：

```
.agent/docs/
├── product-summary.md           # 产品需求和解决方案
├── frontend-spec.md             # 前端开发规范
├── nlp-strategy-design.md       # NLP选股策略设计
├── nlp-strategy-dev-plan.md     # 开发计划和任务拆解
├── akshare-api-guide.md         # AKShare API 集成指南 ⭐新增
└── component-guides/            # 组件开发指南目录
    ├── akshare-integration.md   # AKShare 集成指南
    └── ...
```

## 🔄 开发流程

### 1. 开发前 - 任务规划
```markdown
## 📋 TODO 清单
在开始任何开发任务前，必须先列出：
- [ ] 任务目标
- [ ] 具体步骤（拆解为小任务）
- [ ] 依赖项检查
- [ ] 预期交付物
```

### 2. 开发中
- 遵循 `frontend-spec.md` 中的 UI/UX 规范
- 参考 `product-summary.md` 确认需求
- 每完成一个组件，写开发指南

### 3. 开发后 / 收工流程
- 提交代码到 dev 分支（本地 commit，积累后 push）
- **更新开发指南**（`.agent/docs/component-guides/`）
- **创建每日工作总结** `.agent/workflows/daily-summary-YYYY-MM-DD.md`

### 4. 每日开始
- 跟用户说**早安**
- 查看前一天的工作总结 `/daily-summary-{前一天日期}`
- 确认今日待办事项

## 📝 开发指南模板

每完成一个组件，必须在 `.agent/docs/component-guides/` 创建对应的开发指南：

```markdown
# [组件名称] 开发指南

## 概述
简要描述组件的功能和用途

## 文件结构
- 主要文件路径
- 依赖文件

## API / 接口
- 输入参数
- 返回值
- 使用示例

## 常见问题
- 问题1：...
- 解决方案：...

## 更新日志
- YYYY-MM-DD: 初始版本
```

## 📝 Git 提交规范

```bash
# 功能开发
git commit -m "Feature: 功能描述"

# Bug 修复
git commit -m "Fix: 问题描述"

# 优化改进
git commit -m "Improve: 优化描述"
```

### 分支管理
- `dev`: 日常开发分支
- `main`: 稳定版本，用于 Manus 部署
- 积累多个相关改动后再 push 到远程
- 功能完成后合并 dev → main

## 🛠️ 常用命令

```bash
# 启动开发服务器
npm run dev

# 启动 AKTools 服务（Python）
cd /path/to/pyvenv && source bin/activate && aktools

# 提交代码（本地）
git add -A && git commit -m "描述"

# 推送到远程
git push origin dev
```

## 📋 新功能开发检查清单

- [ ] 创建 TODO 清单
- [ ] 阅读相关设计文档
- [ ] 在 dev 分支开发
- [ ] 代码本地测试通过
- [ ] commit 描述清晰
- [ ] 写组件开发指南
- [ ] 功能完成后更新文档状态

## 📦 依赖管理规范

### 自动升级原则
**看到任何依赖有更新提示时，必须主动升级！** 用户要发布生产环境，必须保持最新版本。

### Python 依赖（AKShare/AKTools）
```bash
# 检查并升级 pip
/path/to/pdfenv/bin/python -m pip install --upgrade pip

# 升级 akshare 和 aktools
/path/to/pdfenv/bin/pip install --upgrade akshare aktools

# 检查版本
/path/to/pdfenv/bin/pip show akshare aktools | grep -E "^(Name|Version)"
```

### Node.js 依赖
```bash
# 检查过时的包
npm outdated

# 升级所有包到最新
npm update

# 或使用 pnpm
pnpm update
```

### 记录更新
每次更新依赖后，在当日工作总结中记录：
```markdown
### 依赖更新
- akshare: 1.18.8 → 1.18.9
- pip: 25.1.1 → 25.3
```

## 🚀 启动命令

```bash
# 一键启动所有服务（推荐）
npm run start:all

# 单独启动
npm run dev              # 主服务器
npm run start:aktools    # AKTools
npm run stop:aktools     # 停止 AKTools
docker-compose up -d     # MySQL
```

