# Stock Tracker 服务端口规范

## 📋 端口分配表

| 端口范围      | 用途     | 服务                     | 状态      |
| ------------- | -------- | ------------------------ | --------- |
| **6888-6897** | 主服务器 | Stock Tracker Web Server | ✅ 使用中 |
| **3306**      | 数据库   | MySQL                    | ✅ 使用中 |
| **8098-8099** | 辅助服务 | AKTools HTTP API         | 📌 规划中 |

---

## 🔧 详细端口说明

### 主服务器 (6888-6897)

| 端口      | 服务          | 说明                       |
| --------- | ------------- | -------------------------- |
| 6888      | Stock Tracker | 主开发服务器 (npm run dev) |
| 6889-6897 | 冗余端口      | 端口冲突时自动切换         |

### 数据库服务 (3306)

| 端口 | 服务  | 说明                    |
| ---- | ----- | ----------------------- |
| 3306 | MySQL | stock-mysql Docker 容器 |

### 辅助服务 (8098+)

| 端口 | 服务    | 说明                  |
| ---- | ------- | --------------------- |
| 8098 | AKTools | AKShare HTTP API 服务 |
| 8099 | 预留    | 未来扩展              |

---

## 🚀 服务启动命令

### 1. MySQL 数据库

```bash
cd /Users/kckylechen/Desktop/Stock\ Tracker/stock-tracker
docker-compose up -d
```

### 2. AKTools HTTP API

```bash
# 方式一：使用启动脚本（推荐）
./scripts/start-aktools.sh

# 方式二：手动启动
cd "/Users/kckylechen/Desktop/Stock Tracker"
./pdfenv/bin/python -m aktools -P 8098
```

### 3. 主服务器

```bash
cd /Users/kckylechen/Desktop/Stock\ Tracker/stock-tracker
npm run dev
```

---

## 📝 环境变量配置

在 `.env` 文件中配置服务地址：

```env
# 数据库
DATABASE_URL=mysql://root:stockpass@localhost:3306/stock_tracker

# AKTools 服务
AKTOOLS_URL=http://127.0.0.1:8098

# 主服务器端口
PORT=6888
```

---

## 🔄 完整启动顺序

1. **启动 MySQL**: `docker-compose up -d`
2. **启动 AKTools**: `./scripts/start-aktools.sh`
3. **启动主服务**: `npm run dev`

---

## ⚠️ 注意事项

1. **端口冲突**: 如果 6888 端口被占用，服务会自动切换到 6889-6897
2. **AKTools 依赖**: AI 助手的龙虎榜、财经资讯等功能依赖 AKTools 服务
3. **Docker 网络**: MySQL 使用 Docker 网络，主服务通过 localhost:3306 访问

---

_更新时间: 2026-01-10_
