# DragonFly AI-First 架构综合参考文档

**日期**: 2026-01-28  
**参与分析**: Gemini, Germanite, Antigravity, Amp, Oracle  
**目的**: 综合所有 AI 建议，形成最终架构决策参考

---

## 📋 一、背景与目标

### 1.1 项目概况

- **项目名称**: DragonFly（蜻蜓）
- **定位**: A股证券分析系统
- **技术栈**: Express + tRPC + MySQL + React 19 + Vite
- **AI**: Grok/GLM 模型

### 1.2 用户的长期目标

1. **AI-First 产品形态**: 从"股票软件驱动"转型为"AI驱动"
2. **小模型编排大模型**: NVIDIA Orchestra 模式
3. **可复用框架**: 封装成不同领域的 Chatbot（股票、资产评估等）
4. **Manus 风格 UI**: 三栏布局（股票池 | AI聊天 | Canvas可视化）

### 1.3 候选技术

- **Moltbot (龙虾机器人)**: 开源 AI 助手平台，17MB monorepo
- **SimpleMem**: 用户自设计的 4 层证券记忆系统
- **6 步分析框架**: 用户自设计的证券分析工作流

---

## 🔍 二、各 AI 分析汇总

### 2.1 Gemini 的建议

**核心观点**: 直接集成 Moltbot 作为后端微服务

```
[前端] → [DragonFly Server] → [Moltbot Gateway]
                                    ↓
                             使用 Moltbot 内置工具
```

**优势**:
- 获得浏览器/联网搜索能力
- 多模型切换开箱即用
- 上下文压缩/修剪

**问题**:
- ❌ Tool Calls 不支持外部回传（经代码验证）
- ❌ 会丢失 DragonFly 的 40+ 股票工具

**采纳度**: ⚠️ 部分采纳（作为外壳而非底座）

---

### 2.2 Germanite 的建议

**核心观点**: Moltbot 做推理，DragonFly 做记忆

```
Dragonfly: SimpleMem 存储 + 6步编排
Moltbot: LLM 推理 + 工具调用 + 浏览器
```

**优势**:
- 分层清晰
- 利用 Moltbot 的浏览器工具

**问题**:
- ⚠️ 依赖 Moltbot 运维
- ⚠️ 边界不够清晰

**采纳度**: ⚠️ 方向对但需要细化

---

### 2.3 Antigravity 的分析

**代码验证发现**:

| Moltbot 模块 | 代码量 | 核心特性 | 复杂度 |
|-------------|--------|---------|--------|
| Cron | ~200 行 | at/every/cron 调度 | 🟢 简洁 |
| Canvas Host | ~480 行 | 静态文件 + LiveReload | 🟢 简洁 |
| Agent Runtime | ~650 行 | Auth轮换/Context压缩 | 🔴 复杂 |
| Gateway | ~20K 行 | 全功能网关 | 🔴 很复杂 |

**关键发现**:
1. Moltbot 的 OpenAI 接口不支持外部 Tool Calls 回传
2. DragonFly 现有 SmartAgent 已有 Session/Memory/Skill 系统
3. DragonFly 的 40+ 股票工具比 Moltbot 通用工具更专业

**建议**: 不集成 Moltbot，在现有架构上增加 SimpleMem + 6步框架

**采纳度**: ✅ 基本采纳

---

### 2.4 Amp 的分析

**功能对比**:

| 能力 | Moltbot | SimpleMem | 结论 |
|------|---------|-----------|------|
| 记忆系统 | ✅ 向量搜索 | ✅ 4层分层记忆 | **SimpleMem 更专业** |
| 会话管理 | ✅ 多频道/子代理 | ❌ 基础 | Moltbot 更强 |
| Skills系统 | ✅ 50+ 预置 | ❌ 无 | Moltbot 更强 |
| 多频道 | ✅ 10+ 渠道 | ❌ 只有 Web | Moltbot 更强 |
| 证券领域 | ❌ 通用 | ✅ 专业 | **SimpleMem 更专业** |

**核心建议**: 不直接集成整个 Moltbot

**原因**:
1. 产品定位不同（个人助手 vs 多用户SaaS）
2. 维护成本高（17MB monorepo）
3. 领域稀释
4. 数据一致性风险

**采纳度**: ✅ 完全采纳

---

### 2.5 Oracle 的分析

**核心结论**:

> **不要把 Moltbot 作为 DragonFly 的"核心底座"直接集成进来**
> 
> DragonFly 的核心竞争力在于 **证券领域结构化记忆（SimpleMem 4层）+ 证券分析工作流**

**推荐架构**: 内核 + 外壳分离

```
┌─────────────────────────────────────────────────┐
│           Moltbot (可选外壳)                     │
│   多频道 / Skills / 向量检索 / 模型 failover     │
└─────────────────────┬───────────────────────────┘
                      │ HTTP/Skill 调用
┌─────────────────────▼───────────────────────────┐
│           DragonFly (领域内核)                   │
│   SimpleMem 4层 / 6步分析 / tRPC API / MySQL    │
└─────────────────────────────────────────────────┘
```

**红线**:
- 事实写入只发生在 MySQL/Drizzle
- Moltbot 只有只读权限
- 证券领域逻辑不进入 Moltbot

**采纳度**: ✅ 完全采纳

---

## 🏗️ 三、最终架构决策

### 3.1 架构原则

1. **DragonFly = 领域内核**（必须自主掌控）
2. **Moltbot = 可选外壳**（按需接入，不绑死）
3. **SimpleMem = 结构化记忆**（不是向量记忆的替代品）
4. **边界清晰**：结构化数据 vs 非结构化检索分离

### 3.2 最终架构图

```
┌────────────────────────────────────────────────────────────┐
│                    DragonFly Server                        │
│ ┌────────────────────────────────────────────────────────┐ │
│ │              6步分析框架                               │ │
│ │  Step1: parseInput → Step2: reviewMarket               │ │
│ │  Step3: analyze    → Step4: assess                     │ │
│ │  Step5: advise     → Step6: personalize                │ │
│ └────────────────────────────────────────────────────────┘ │
│ ┌────────────────────────────────────────────────────────┐ │
│ │              SimpleMem 4层记忆系统                     │ │
│ │  ├─ AnalysisMemory (快照级，7天)                       │ │
│ │  ├─ ContextMemory (用户级，长期)                       │ │
│ │  ├─ PortfolioMemory (持仓级，永久)                     │ │
│ │  └─ OperationMemory (模式级，长期)                     │ │
│ └────────────────────────────────────────────────────────┘ │
│ ┌────────────────────────────────────────────────────────┐ │
│ │              现有系统（保留）                          │ │
│ │  ├─ SmartAgent + Orchestrator                          │ │
│ │  ├─ 40+ 股票工具 (AKShare)                             │ │
│ │  └─ Session + Memory + Skill                           │ │
│ └────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
```

### 3.3 未来扩展架构

```
ai-core/                    # 跨项目通用能力
├── llm-client/             # LLM client + provider/failover
├── tools/                  # 工具/技能接口 schema
├── retrieval/              # 非结构化检索接口
└── observability/          # 日志、trace、成本统计

dragonfly/                  # 证券项目
├── domain/                 # SimpleMem + 6步分析
├── api/                    # tRPC
└── web/                    # React

report-bot/                 # 资产评估项目（未来）
├── domain/
├── api/
└── channels/               # 可选接 Moltbot
```

---

## 📊 四、SimpleMem 4层记忆系统

> 详细设计见: [SimpleMem-Memory-Design.md](../futureshop/SimpleMem-Memory-Design.md)

### 4.1 四层概览

| 记忆层 | 粒度 | 生命周期 | 用途 |
|--------|------|---------|------|
| **AnalysisMemory** | 快照级 | 7天 | 识别用户整体风险和情绪 |
| **ContextMemory** | 用户级 | 长期 | 维护用户画像和对话上下文 |
| **PortfolioMemory** | 持仓级 | 永久 | 跟踪单支股票的完整历史 |
| **OperationMemory** | 模式级 | 长期 | 学习操作成功/失败特征 |

### 4.2 核心数据结构

```typescript
// ContextMemory
interface UserProfile {
  userId: string;
  tradingExperience: 'beginner' | 'intermediate' | 'advanced';
  preferredStrategy: 'technical' | 'fundamental' | 'sentiment' | 'mixed';
  learningHistory: { frequentMistakes, improvementAreas, successfulPatterns };
  preferences: { analysisDetailLevel, questionStyle, dataVisualization };
  riskProfile: { baseSelfAssessedRiskTolerance, behavioralRiskTolerance };
}

// PortfolioMemory
interface HoldingMemory {
  code: string;
  costBasis: { totalShares, avgPrice, totalCost, costLayers[] };
  transactionHistory: { date, action, shares, price, reason }[];
  psychologicalExpectation: { targetPrice, stopLossPrice, confidenceLevel };
  reviewResult?: { realizedPnl, lessonsLearned, outcome };
}

// OperationMemory
interface OperationPattern {
  operationType: 'chaseHigh' | 'cuttingLoss' | 'technicalBreakout' | ...;
  successMetrics: { totalAttempts, successCount, winRate, profitFactor };
  triggerConditions: { marketCondition, technicalSignals, sentiment };
  personalReview: { whenUserSucceeds[], whenUserFails[], recommendation };
}
```

---

## 📋 五、6步分析框架

> 详细设计见: [stock-tracker-analysis-framework.md](../futureshop/stock-tracker-analysis-framework.md)

### 5.1 六步流程

```
Step 1: parseUserInput     → 读懂输入，提取股票代码/问题类型/隐含心态
Step 2: reviewMarketStatus → 获取实时行情，对比历史
Step 3: multidimensionalAnalysis → 技术面/资金面/基本面/情绪面
Step 4: riskRewardAssessment → 风险收益评估，对比用户承受能力
Step 5: generateOperationalAdvice → 短/中/长线建议 + 具体点位
Step 6: personalizedQAAndAdjustment → 个性化调整 + 智能提问
```

### 5.2 与 SimpleMem 的交互

| 步骤 | 读取 | 写入 |
|------|------|------|
| Step 1 | ContextMemory.knownFacts | ContextMemory.newInformation |
| Step 2 | PortfolioMemory.costBasis | AnalysisMemory.marketSnapshot |
| Step 3 | OperationMemory.patterns | AnalysisMemory.analysisSnapshot |
| Step 4 | UserMindset.riskTolerance | AnalysisMemory.riskAssessment |
| Step 5 | PortfolioMemory.psychologicalExpectation | AnalysisMemory.generatedAdvice |
| Step 6 | 全部记忆 | FinalRecommendation |

---

## 🔧 六、从 Moltbot 借鉴的概念

即使不直接集成，以下概念值得吸收：

### 6.1 Hybrid 检索（BM25 + 向量）

```typescript
// 股票代码/指标 → BM25（精确匹配）
// 结论/观点 → 向量（语义相近）
mergeHybridResults(vectorResults, bm25Results)
```

### 6.2 增量索引与成本控制

- 阈值触发 + 批处理 + 重试 + 缓存
- 控制 embedding 成本

### 6.3 Embedding Provider 抽象

把模型选择/密钥/故障处理从业务剥离，未来多项目复用。

### 6.4 技能系统（插件化边界）

- 清晰的 skill manifest（能力、权限、输入输出 schema）
- 动态安装/版本管理

### 6.5 Session/Transcript 一等公民

对话记录、摘要、决策链路纳入可回放/可审计的数据结构。

---

## 🚀 七、实施路线图

### 7.1 短期（DragonFly 完善）

| Phase | 任务 | 工作量 | 优先级 |
|-------|------|--------|--------|
| **1** | SimpleMem 4层数据库 | 1-2天 | 🔴 高 |
| **2** | 6步分析框架 | 2-3天 | 🔴 高 |
| **3** | Manus 三栏 UI | 3-5天 | 🔴 高 |
| **4** | Cron 调度器 | 2-3天 | 🟡 中 |

### 7.2 长期（DragonCore 抽象）

| Phase | 任务 | 说明 |
|-------|------|------|
| **A** | 抽取 ai-core 共享层 | LLM client / Tools / Retrieval |
| **B** | 实现 Orchestra 编排器 | 小模型路由大模型 |
| **C** | 迁移为 @dragon/plugin-stock | 插件化 |
| **D** | 创建资产评估插件 | 复用框架 |
| **E** | Moltbot Skill PoC | 可选外壳接入 |

---

## ⚠️ 八、风险与护栏

| 风险 | 应对措施 |
|------|---------|
| 产品定位错配 | Moltbot 是个人助手，不硬融合多用户场景 |
| 数据一致性 | SimpleMem 是权威源，向量索引只是加速器 |
| 多租户/权限 | 最小权限 token + 写操作确认 |
| 维护成本 | 不把 17MB monorepo 混进主仓库 |

### 红线

- ❌ 事实写入只发生在 MySQL/Drizzle
- ❌ Moltbot 只有只读权限
- ❌ 证券领域逻辑不进入 Moltbot

---

## 🐉 十、Gemini 的 DragonCore 框架设计

> 详细设计见: [AI Agent 核心框架设计.docx](./AI%20Agent%20核心框架设计.docx)

这是 Gemini 提供的一份非常完整的企业级 Agent 框架蓝图。

### 10.1 核心理念

**Orchestra Pattern**: 小模型路由，大模型执行

> NVIDIA 研究表明，8B 参数模型在工具选择和任务路由上可媲美 GPT-4，但成本仅为后者的几十分之一。

### 10.2 Monorepo 目录结构

```
dragon-core/
├── apps/                   # 应用层
│   ├── gateway/            # 核心网关 (Fastify)
│   ├── dashboard/          # 监控看板 (Next.js)
│   ├── worker-python/      # Python 执行环境
│   └── worker-node/        # Node.js 执行环境
│
├── packages/               # 核心库层
│   ├── core/               # 框架内核
│   │   ├── orchestra/      # 编排引擎
│   │   ├── memory/         # SimpleMem 实现
│   │   └── telemetry/      # OpenTelemetry
│   ├── protocol/           # 协议定义 (Zod Schemas)
│   ├── plugins/            # 标准化插件集
│   └── llm-adapter/        # 模型适配层
│
└── infrastructure/         # 基础设施即代码
```

### 10.3 Plugin 接口设计

```typescript
interface DragonTool<I, O> {
  id: string;                    // 如 'finance::get_stock_price'
  name: string;
  description: string;
  parameters: I;                 // Zod Schema
  outputSchema: O;               // Zod Schema
  hints: ToolRoutingHints;       // 路由元数据
  execute(args, context): Promise<O>;
}

interface ToolRoutingHints {
  complexity: 'low' | 'medium' | 'high';
  domain: 'finance' | 'coding' | 'general' | ...;
  latency: 'realtime' | 'batch';
  costWeight: number;
}
```

### 10.4 Orchestra 编排器工作流

1. **感知**: Router 接收用户查询 + SimpleMem 检索的记忆
2. **规划**: Router 将任务分解为步骤
3. **路由**: 根据 ToolRoutingHints 决定：
   - 简单任务 → Router 直接执行
   - 复杂任务 → 委派给 Executor (GPT-4o)
4. **执行**: 调用工具获取结果
5. **反思**: Critic 检查结果，失败则重试

### 10.5 混合生成架构（长报告）

```
全局规划 → 并行微批执行 → 流式交付 → 审核缓冲
    ↓           ↓            ↓           ↓
  大纲生成   Worker批处理   WebSocket   Critic审查
```

### 10.6 技术路线图

| 阶段 | 目标 | 时间 |
|------|------|------|
| **1** | 地基重构: Monorepo + Plugin 机制 | Month 1-2 |
| **2** | 大脑升级: Orchestra Engine | Month 3-4 |
| **3** | 记忆植入: SimpleMem 集成 | Month 5-6 |
| **4** | 企业加固: 安全/可观测/HITL | Month 7+ |

### 10.7 与当前计划的关系

Gemini 的设计是**长期愿景**，我们的短期计划是其**子集**：

| 短期计划 | 对应 Gemini 设计 |
|---------|-----------------|
| SimpleMem 4层 | Phase 3 Memory |
| 6步分析框架 | packages/plugins/finance |
| Manus UI | apps/dashboard |
| Orchestra 编排器 | Phase 2 Brain |

---

## 🦞 十一、Gemini 混合架构决策分析

> 详细设计见: [龙虾机器人重构或直接使用决策.docx](./龙虾机器人重构或直接使用决策.docx)

### 11.1 核心结论

> **二元对立（重构 vs. 采用）是错误的**
> 
> 最优解是将 Moltbot 视为底层的"操作系统"，而将 DragonFly 升级为运行其上的"杀手级应用"。

### 11.2 混合架构蓝图

```
┌─────────────────────────────────────────────┐
│        Moltbot Gateway (操作系统)            │
│  - 多渠道接入 (Discord/Telegram/WhatsApp)   │
│  - 消息路由                                  │
│  - 基础 Shell/文件操作                       │
└─────────────────┬───────────────────────────┘
                  │ MCP Protocol
┌─────────────────▼───────────────────────────┐
│        DragonFly MCP Server (应用层)         │
│  ├─ SimpleMem 记忆系统                       │
│  ├─ LangGraph 循环推理                       │
│  ├─ Semantic Router 语义路由                 │
│  └─ 40+ 金融工具 (AKShare)                   │
└─────────────────────────────────────────────┘
```

**职责分工**：
- **Moltbot** = "耳朵"和"嘴巴"（负责听和说）
- **DragonFly** = "大脑"和"手"（负责思考和操作）

### 11.3 原生 Moltbot vs 混合架构对比

| 维度 | 原生 Moltbot | 混合架构 |
|------|-------------|---------|
| 多渠道接入 | ✅ 原生支持 | ✅ 继承 |
| 记忆机制 | ❌ 基础 RAG | ✅ SimpleMem |
| 推理模式 | ❌ 线性 ReAct | ✅ LangGraph 循环 |
| 成本优化 | ❌ 全量 Token | ✅ 语义路由分流 |
| 金融工具 | ❌ 通用 | ✅ 专业 40+ |
| 安全隔离 | ❌ 高权限风险 | ✅ Docker 沙盒 |

### 11.4 三大技术增强

**A. SimpleMem 记忆机制**
```
写入 → 语义压缩 → 原子事实
             ↓
      递归整合 → 用户画像
             ↓
读取 ← 自适应检索 (深度/广度动态调整)
```

**B. LangGraph 循环工作流**
```
Plan → Research → Analyze → Draft → Critique
  ↑                                    │
  └────────────────────────────────────┘
         (不达标则重新研究)
```

**C. Semantic Router 语义路由**
- 快速通道：简单查询 → 直接调用工具（毫秒级）
- 慢速通道：复杂分析 → LangGraph 深度思考

### 11.5 实施路线图

| 阶段 | 任务 | 时间 |
|------|------|------|
| **1** | 容器化 Moltbot + MCP 化 DragonFly | 1-2 周 |
| **2** | SimpleMem 实现 + 语义路由 | 3-4 周 |
| **3** | LangGraph 多步推理 + 用户体验优化 | 1-2 月 |

---

## 📎 十二、参考文档

| 文档 | 路径 |
|------|------|
| SimpleMem 设计 | [SimpleMem-Memory-Design.md](../futureshop/SimpleMem-Memory-Design.md) |
| 6步分析框架 | [stock-tracker-analysis-framework.md](../futureshop/stock-tracker-analysis-framework.md) |
| Amp 分析报告 | [Moltbot-Integration-Analysis.md](./Moltbot-Integration-Analysis.md) |
| Gemini DragonCore 设计 | [AI Agent 核心框架设计.docx](./AI%20Agent%20核心框架设计.docx) |
| Gemini 混合架构决策 | [龙虾机器人重构或直接使用决策.docx](./龙虾机器人重构或直接使用决策.docx) |
| Moltbot 源码 | `/tmp/moltbot-reference/` 或 GitHub |

---

**文档维护者**: AI Team (Gemini/Germanite/Antigravity/Amp/Oracle)  
**最后更新**: 2026-01-28
