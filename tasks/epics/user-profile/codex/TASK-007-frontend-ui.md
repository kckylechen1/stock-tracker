# TASK-007: 前端用户切换组件

## 负责 Agent: 🟢 Codex
> React 前端组件开发，Codex 更擅长前端框架。

## 背景 (Why)
用户需要在界面上切换用户、添加新用户，并完成问诊流程。

## 目标 (Done Definition)
- [ ] 用户切换下拉按钮（显示当前用户，支持切换）
- [ ] 添加新用户按钮 + 输入昵称对话框
- [ ] 问诊对话框（ProfileModal）- 对话式收集信息
- [ ] 集成到 `DashboardLayout.tsx` 顶部
- [ ] 新用户首次使用自动弹出问诊
- [ ] CI 全绿

## 范围
**In-scope:**
- UserSwitch 组件
- ProfileModal 问诊对话框
- 与后端 API 集成

**Out-of-scope:**
- 后端逻辑（TASK-004/005/006）

## 设计稿

```
┌──────────────────────────────────────────────────────────────────┐
│ 🐉 DragonFly                              [👤 用户名 ▼] [设置]   │
│                                            ├─ 切换用户1         │
│                                            ├─ 切换用户2         │
│                                            ├─────────────       │
│                                            └─ ➕ 添加新用户     │
└──────────────────────────────────────────────────────────────────┘

问诊对话框:
┌──────────────────────────────────────────────────────────────────┐
│                     👋 欢迎使用 DragonFly                        │
│                                                                  │
│  让我了解一下您的交易风格，以便提供更好的建议                    │
│                                                                  │
│  ────────────────────────────────────────────────────────────── │
│                                                                  │
│  问题 1/5                                                        │
│                                                                  │
│  怎么称呼您？                                                    │
│                                                                  │
│  ┌────────────────────────────────────────┐                     │
│  │                                        │                     │
│  └────────────────────────────────────────┘                     │
│                                                                  │
│                                           [下一步 →]            │
│                                                                  │
└──────────────────────────────────────────────────────────────────┘
```

## 契约 (React Component Props)

```typescript
// client/src/components/UserSwitch.tsx

interface UserSwitchProps {
  className?: string;
}

// client/src/components/ProfileModal.tsx

interface ProfileModalProps {
  open: boolean;
  onClose: () => void;
  userId: string;
  onComplete: (profile: UserProfile) => void;
}
```

## 实施计划

1. 创建 `client/src/components/UserSwitch.tsx`
   - 下拉菜单显示用户列表
   - 切换用户调用 `trpc.profile.switchUser`
   - 添加新用户弹出输入框

2. 创建 `client/src/components/ProfileModal.tsx`
   - 问诊对话界面
   - 调用 `trpc.profile.startProfiling`
   - 逐个显示问题，收集回答
   - 调用 `trpc.profile.answerQuestion`
   - 完成后调用 `trpc.profile.completeProfiling`

3. 修改 `client/src/components/DashboardLayout.tsx`
   - 在顶部栏添加 UserSwitch 组件
   - 新用户自动弹出 ProfileModal

4. 添加必要的 UI 组件（如果需要）

## 验收清单

- [ ] 能看到用户切换按钮
- [ ] 能添加新用户
- [ ] 问诊对话框正常工作
- [ ] 问诊完成后 Profile 保存成功

## 文件清单

| 操作 | 文件路径 |
|------|----------|
| CREATE | `client/src/components/UserSwitch.tsx` |
| CREATE | `client/src/components/ProfileModal.tsx` |
| MODIFY | `client/src/components/DashboardLayout.tsx` |

## 依赖

- TASK-004 完成（API 可用）
- TASK-006 完成（问诊 API 可用）

## UI 组件依赖

```typescript
// 可能需要的 shadcn/ui 组件
import { 
  DropdownMenu, 
  DropdownMenuContent, 
  DropdownMenuItem, 
  DropdownMenuTrigger,
  Dialog,
  DialogContent,
  DialogHeader,
  DialogTitle,
  Input,
  Button,
  RadioGroup,
  RadioGroupItem,
  Progress
} from "@/components/ui/*";
```

## 进度日志

| 时间 | Agent | 动作 | 产物 |
|------|-------|------|------|
| 2026-01-17 | Amp | 创建任务 | TASK-007-frontend-ui.md |
